# Maintained in https://github.com/coreos/repo-templates
# Do not edit downstream.

name: Rust
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened, labeled, unlabeled]
permissions:
  contents: read

# don't waste job slots on superseded code
concurrency:
  group: {% raw %}${{ github.workflow }}-${{ github.ref }}{% endraw %}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
{%- if msrv != "auto" %}
  # Minimum supported Rust version (MSRV)
  MSRV: {{ msrv }}
{%- endif %}
  # Pinned toolchain for linting
  ACTIONS_LINTS_TOOLCHAIN: {{ lint_toolchain }}

jobs:
  tests-stable:
    name: Tests, stable toolchain
    runs-on: ubuntu-latest
    container: quay.io/coreos-assembler/fcos-buildroot:testing-devel
    steps:
      - name: Check out repository
        uses: actions/checkout@v6
      - name: Install toolchain
        uses: dtolnay/rust-toolchain@v1
        with:
          toolchain: stable
      - name: Cache build artifacts
        uses: Swatinem/rust-cache@v2
      - name: cargo build
        run: cargo build --all-targets
      - name: cargo test
        run: cargo test --all-targets
{%- for feature in extended_test_features|default(value=[]) %}
      - name: cargo test ({{ feature }})
        run: cargo test --all-targets --features {{ feature }}
{%- endfor %}
  tests-release-stable:
    name: Tests (release), stable toolchain
    runs-on: ubuntu-latest
    container: quay.io/coreos-assembler/fcos-buildroot:testing-devel
    steps:
      - name: Check out repository
        uses: actions/checkout@v6
      - name: Install toolchain
        uses: dtolnay/rust-toolchain@v1
        with:
          toolchain: stable
      - name: Cache build artifacts
        uses: Swatinem/rust-cache@v2
      - name: cargo build (release)
        run: cargo build --all-targets --release
      - name: cargo test (release)
        run: cargo test --all-targets --release
  tests-release-msrv:
    name: Tests (release), minimum supported toolchain
    runs-on: ubuntu-latest
    container: quay.io/coreos-assembler/fcos-buildroot:testing-devel
    steps:
      - name: Check out repository
        uses: actions/checkout@v6
{%- if msrv == "auto" %}
      - name: Detect crate MSRV
        run: |
          msrv=$(cargo metadata --format-version 1 --no-deps | \
              jq -r '.packages[0].rust_version')
          echo "Crate MSRV: $msrv"
          echo "MSRV=$msrv" >> $GITHUB_ENV
{%- endif %}
      - name: Install toolchain
        uses: dtolnay/rust-toolchain@v1
        with:
          toolchain: {% raw %}${{ env.MSRV }}{% endraw %}
      - name: Cache build artifacts
        uses: Swatinem/rust-cache@v2
      - name: cargo build (release)
        run: cargo build --all-targets --release
      - name: cargo test (release)
        run: cargo test --all-targets --release
  linting:
    name: Lints, pinned toolchain
    runs-on: ubuntu-latest
    container: quay.io/coreos-assembler/fcos-buildroot:testing-devel
    steps:
      - name: Check out repository
        uses: actions/checkout@v6
      - name: Install toolchain
        uses: dtolnay/rust-toolchain@v1
        with:
          toolchain: {% raw %}${{ env.ACTIONS_LINTS_TOOLCHAIN }}{% endraw %}
          components: rustfmt, clippy
      - name: Cache build artifacts
        uses: Swatinem/rust-cache@v2
      - name: cargo fmt (check)
        run: cargo fmt -- --check -l
      - name: cargo clippy (warnings)
        run: cargo clippy --all-targets -- -D warnings
  check-lockfile-downgrades:
    name: Check for Cargo.lock downgrades
    runs-on: ubuntu-latest
    if: >-
      github.event_name == 'pull_request' &&
      !contains(github.event.pull_request.labels.*.name, 'allow-downgrade')
    steps:
      - name: Check out repository
        uses: actions/checkout@v6
      - name: Check for base lockfile
        id: base-lockfile
        env:
          BASE_REF: {% raw %}${{ github.base_ref }}{% endraw %}
        run: |
          set -euo pipefail
          git fetch --depth=1 origin "+refs/heads/$BASE_REF:refs/remotes/origin/$BASE_REF"
          if git cat-file -e "origin/$BASE_REF:Cargo.lock"; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "No Cargo.lock on base branch; skipping downgrade check."
          fi
      - name: Check for dependency downgrades
        id: downgrades
        if: steps.base-lockfile.outputs.exists == 'true'
        uses: Garfield1002/cargo-no-downgrades@5adc12fa7e8484939ba064ac4fd4fb5333d8f5de # v1.0.0
      - name: Explain intentional downgrade override
        if: failure() && steps.downgrades.outcome == 'failure'
        run: |
          echo "If the downgrade is intentional, add the 'allow-downgrade' label and document the reason in the PR description."
  tests-other-channels:
    name: Tests, unstable toolchain
    runs-on: ubuntu-latest
    container: quay.io/coreos-assembler/fcos-buildroot:testing-devel
    continue-on-error: true
    strategy:
      matrix:
        channel: [beta, nightly]
    steps:
      - name: Check out repository
        uses: actions/checkout@v6
      - name: Install toolchain
        uses: dtolnay/rust-toolchain@v1
        with:
          toolchain: {% raw %}${{ matrix.channel }}{% endraw %}
      - name: Cache build artifacts
        uses: Swatinem/rust-cache@v2
      - name: cargo build
        run: cargo build --all-targets
      - name: cargo test
        run: cargo test --all-targets
