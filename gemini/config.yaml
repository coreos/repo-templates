vars:
  branches: [main]

files:
  - repo: afterburn
    path: .gemini/config.yaml

  - repo: bootupd
    path: .gemini/config.yaml

  - repo: butane
    path: .gemini/config.yaml

  - repo: chunkah
    path: .gemini/config.yaml

  - repo: coreos-assembler
    path: .gemini/config.yaml

  - repo: coreos-installer
    path: .gemini/config.yaml

  - repo: fedora-coreos-config
    path: .gemini/config.yaml

  - repo: fedora-coreos-pipeline
    path: .gemini/config.yaml

  - repo: ignition
    path: .gemini/config.yaml

  - repo: rhel-coreos-config
    path: .gemini/config.yaml

  - repo: rpm-ostree
    path: .gemini/config.yaml

  - repo: zincati
    path: .gemini/config.yaml
